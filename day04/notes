Notes

Today I learned essential Linux commands used for file management:

* touch → Create files
* mkdir → Create folders
* cp → Copy files
* mv → Move/rename files
* rm → Delete files
* cat → View file content
* grep → Search inside files

I also learned that grep is very powerful for searching logs and detecting suspicious activity.
